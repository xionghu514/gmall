spring:
  application:
    name: sms-service # 服务名称
  cloud:
    nacos:
      config:
        server-addr: 192.168.0.101:8848 # 配置中心地址
        namespace: a28f4a0e-5f47-49af-bd33-7cbd740c58f9 # 命名空间 ID
        group: dev # 配置文件分组
        file-extension: yml # 配置文件扩展名, 默认是 properties